apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: "https://acme-v02.api.letsencrypt.org/directory"
    email: "ka-sharif@outlook.com"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z00176861AUERXFO9TXIH
          region: eu-west-2
      selector:
        dnsZones:
        - labs.vettlyai.com
    - http01:
        ingress:
          class: nginx